name: Deploy to Google Cloud

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.12'
      # - name: Install Dependencies
      #   run: |
      #         python -m venv venv
      #         source ./venv/bin/activate
      #         pip install --upgrade pip
      #         pip install -r requirements.txt

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_IP }}  # Your instance's external IP 34.163.154.210
          username: ${{ secrets.SERVER_USER }}  # e.g., "onyejiakamadu" 
          password: ${{ secrets.SSH_PRIVATE_KEY }}  # Private key matching the instance’s authorized_keys
          port: 22
          script: |
            cd ${{ secrets.PROJECT_PATH }}  
            git pull origin main
            touch test
            sudo systemctl restart fastapi-app.service
